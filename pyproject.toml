[project]
name = "expedia-ranking"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "pandas>=2.0",
    "numpy>=1.24",
    "scikit-learn>=1.3",
    "lightgbm>=4.0",
    "matplotlib>=3.7",
    "seaborn>=0.12",
    "pyarrow>=15.0",
]

[tool.uv]
dev-dependencies = ["jupyter>=1.0", "ipykernel>=6.0"]
